apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  name: "prodteamdbupdatep3rcdn"
spec:
  storageClassName: "standard"
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "1Gi"

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: "prodteamdbupdatep3rcdn"
spec:
  replicas: 1
  selector:
    matchLabels:
      name: "prodteamdbupdatep3rcdn"
  template:
    metadata:
      labels:
        name: "prodteamdbupdatep3rcdn"
    spec:
      containers:
        -
          name: "prodteamdbupdatep3rcdn"
          image: "containers.cisco.com/csb-cnt-dev/teamsdbupdate:teamupdateprodrcdn"


          volumeMounts:
            - name: "storage"
              mountPath: "/opt/app-root/src/logs"

          imagePullPolicy: "Always"

          securityContext:
            capabilities: {}
            privileged: false
      volumes:
        -
          name: "storage"
          persistentVolumeClaim:
           claimName: "prodteamdbupdatep3rcdn"

      restartPolicy: "Always"
      dnsPolicy: "ClusterFirst"
  Triggers:
    -
      type: "ConfigChange"
  strategy:
    type: "RollingUpdate"
    rollingParams:
      updatePeriodSeconds: 1
      intervalSeconds: 1
      timeoutSeconds: 420
