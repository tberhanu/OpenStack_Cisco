apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  name: "csbauditor3"
spec:
  storageClassName: "standard"
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "1Gi"

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: "csbauditor3"
spec:
  replicas: 1
  selector:
    matchLabels:
      name: "csbauditor3"
  template:
    metadata:
      labels:
        name: "csbauditor3"
    spec:
      containers:
        -
          name: "csbauditor3"
          image: "containers.cisco.com/csb-cnt-dev/csbauditor:csbauditor_prod"

          volumeMounts:
            - name: "storage"
              mountPath: "/opt/app-root/src/logs"

          imagePullPolicy: "Always"

          securityContext:
            capabilities: {}
            privileged: false
      volumes:
        -
          name: "storage"
          persistentVolumeClaim:
           claimName: "csbauditor3"

      restartPolicy: "Always"
      dnsPolicy: "ClusterFirst"
  Triggers:
    -
      type: "ConfigChange"
  strategy:
    type: "RollingUpdate"
    rollingParams:
      updatePeriodSeconds: 1
      intervalSeconds: 1
      timeoutSeconds: 420
