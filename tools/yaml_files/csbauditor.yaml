apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  name: "csbauditor"
spec:
  storageClassName: "standard"
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "1Gi"

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: "csbauditor"
spec:
  replicas: 1
  selector:
    matchLabels:
      name: "csbauditor"
  template:
    metadata:
      labels:
        name: "csbauditor"
    spec:
      containers:
        -
          name: "csbauditor"
          image: "containers.cisco.com/amardkum/csb_cnt:csbauditor_py36"

          volumeMounts:
            - name: "storage"
              mountPath: "/opt/app-root/src/logs"

          imagePullPolicy: "IfNotPresent"
          securityContext:
            capabilities: {}
            privileged: false
      volumes:
        -
          name: "storage"
          persistentVolumeClaim:
           claimName: "csbauditor"

      restartPolicy: "Always"
      dnsPolicy: "ClusterFirst"
  Triggers:
    -
      type: "ConfigChange"
  strategy:
    type: "RollingUpdate"
    rollingParams:
      updatePeriodSeconds: 1
      intervalSeconds: 1
      timeoutSeconds: 420
