apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  name: "prodteamupdatecaenonprod"
spec:
  storageClassName: "standard"
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "1Gi"

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: "prodteamupdatecaenonprod"
spec:
  replicas: 1
  selector:
    matchLabels:
      name: "prodteamupdatecaenonprod"
  template:
    metadata:
      labels:
        name: "prodteamupdatecaenonprod"
    spec:
      containers:
        -
          name: "prodteamupdatecaenonprod"
          image: "containers.cisco.com/csb-cnt-dev/teamsdbupdate:teamupdateprod_nonprodcae"


          volumeMounts:
            - name: "storage"
              mountPath: "/opt/app-root/src/logs"

          imagePullPolicy: "Always"

          securityContext:
            capabilities: {}
            privileged: false
      volumes:
        -
          name: "storage"
          persistentVolumeClaim:
           claimName: "prodteamupdatecaenonprod"

      restartPolicy: "Always"
      dnsPolicy: "ClusterFirst"
  Triggers:
    -
      type: "ConfigChange"
  strategy:
    type: "RollingUpdate"
    rollingParams:
      updatePeriodSeconds: 1
      intervalSeconds: 1
      timeoutSeconds: 420
