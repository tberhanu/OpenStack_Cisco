apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  name: "nonprodteamsdb"
spec:
  storageClassName: "standard"
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "1Gi"

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: "nonprodteamsdb"
spec:
  replicas: 1
  selector:
    matchLabels:
      name: "nonprodteamsdb"
  template:
    metadata:
      labels:
        name: "nonprodteamsdb"
    spec:
      containers:
        -
          name: "nonprodteamsdb"
          image: "containers.cisco.com/csb-cnt-dev/teamsdbupdate:nonprodteamsdb"

          volumeMounts:
            - name: "storage"
              mountPath: "/opt/app-root/src/logs"

          imagePullPolicy: "Always"

          securityContext:
            capabilities: {}
            privileged: false
      volumes:
        -
          name: "storage"
          persistentVolumeClaim:
           claimName: "nonprodteamsdb"

      restartPolicy: "Always"
      dnsPolicy: "ClusterFirst"
  Triggers:
    -
      type: "ConfigChange"
  strategy:
    type: "RollingUpdate"
    rollingParams:
      updatePeriodSeconds: 1
      intervalSeconds: 1
      timeoutSeconds: 420
