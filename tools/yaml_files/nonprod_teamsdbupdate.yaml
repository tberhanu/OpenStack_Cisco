apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  name: "nonprodteamsdbupdatep3cae"
spec:
  storageClassName: "standard"
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "1Gi"

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: "nonprodteamsdbupdatep3cae"
spec:
  replicas: 1
  selector:
    matchLabels:
      name: "nonprodteamsdbupdatep3cae"
  template:
    metadata:
      labels:
        name: "nonprodteamsdbupdatep3cae"
    spec:
      containers:
        -
          name: "nonprodteamsdbupdatep3cae"
          image: "containers.cisco.com/csb-cnt-dev/teamsdbupdate:teamupdatenonprodp3cae"

          volumeMounts:
            - name: "storage"
              mountPath: "/opt/app-root/src/logs"

          imagePullPolicy: "Always"

          securityContext:
            capabilities: {}
            privileged: false
      volumes:
        -
          name: "storage"
          persistentVolumeClaim:
           claimName: "nonprodteamsdbupdatep3cae"

      restartPolicy: "Always"
      dnsPolicy: "ClusterFirst"
  Triggers:
    -
      type: "ConfigChange"
  strategy:
    type: "RollingUpdate"
    rollingParams:
      updatePeriodSeconds: 1
      intervalSeconds: 1
      timeoutSeconds: 420
