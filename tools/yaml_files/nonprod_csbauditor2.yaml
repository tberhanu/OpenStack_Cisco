apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  name: "nonprodcsbauditor2"
spec:
  storageClassName: "standard"
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "1Gi"

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: "nonprodcsbauditor2"
spec:
  replicas: 1
  selector:
    matchLabels:
      name: "nonprodcsbauditor2"
  template:
    metadata:
      labels:
        name: "nonprodcsbauditor2"
    spec:
      containers:
        -
          name: "nonprodcsbauditor2"
          image: "containers.cisco.com/csb-cnt-dev/csbauditor:csbauditor_nonprod"

          volumeMounts:
            - name: "storage"
              mountPath: "/opt/app-root/src/logs"

          imagePullPolicy: "Always"

          securityContext:
            capabilities: {}
            privileged: false
      volumes:
        -
          name: "storage"
          persistentVolumeClaim:
           claimName: "nonprodcsbauditor2"

      restartPolicy: "Always"
      dnsPolicy: "ClusterFirst"
  Triggers:
    -
      type: "ConfigChange"
  strategy:
    type: "RollingUpdate"
    rollingParams:
      updatePeriodSeconds: 1
      intervalSeconds: 1
      timeoutSeconds: 420
