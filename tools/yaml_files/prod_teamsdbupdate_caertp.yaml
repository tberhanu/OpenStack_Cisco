apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  name: "prodteamdbupdatecaertp"
spec:
  storageClassName: "standard"
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "1Gi"

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: "prodteamdbupdatecaertp"
spec:
  replicas: 1
  selector:
    matchLabels:
      name: "prodteamdbupdatecaertp"
  template:
    metadata:
      labels:
        name: "prodteamdbupdatecaertp"
    spec:
      containers:
        -
          name: "prodteamdbupdatecaertp"
          image: "containers.cisco.com/csb-cnt-dev/teamsdbupdate:teamupdateprodcaertp"


          volumeMounts:
            - name: "storage"
              mountPath: "/opt/app-root/src/logs"

          imagePullPolicy: "Always"

          securityContext:
            capabilities: {}
            privileged: false
      volumes:
        -
          name: "storage"
          persistentVolumeClaim:
           claimName: "prodteamdbupdatecaertp"

      restartPolicy: "Always"
      dnsPolicy: "ClusterFirst"
  Triggers:
    -
      type: "ConfigChange"
  strategy:
    type: "RollingUpdate"
    rollingParams:
      updatePeriodSeconds: 1
      intervalSeconds: 1
      timeoutSeconds: 420
