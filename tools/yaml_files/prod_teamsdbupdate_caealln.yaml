apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  name: "prodteamdbupdatecaealln"
spec:
  storageClassName: "standard"
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "1Gi"

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: "prodteamdbupdatecaealln"
spec:
  replicas: 1
  selector:
    matchLabels:
      name: "prodteamdbupdatecaealln"
  template:
    metadata:
      labels:
        name: "prodteamdbupdatecaealln"
    spec:
      containers:
        -
          name: "prodteamdbupdatecaealln"
          image: "containers.cisco.com/csb-cnt-dev/teamsdbupdate:teamupdateprodcaealln"


          volumeMounts:
            - name: "storage"
              mountPath: "/opt/app-root/src/logs"

          imagePullPolicy: "Always"

          securityContext:
            capabilities: {}
            privileged: false
      volumes:
        -
          name: "storage"
          persistentVolumeClaim:
           claimName: "prodteamdbupdatecaealln"

      restartPolicy: "Always"
      dnsPolicy: "ClusterFirst"
  Triggers:
    -
      type: "ConfigChange"
  strategy:
    type: "RollingUpdate"
    rollingParams:
      updatePeriodSeconds: 1
      intervalSeconds: 1
      timeoutSeconds: 420
